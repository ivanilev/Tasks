import React, { Component } from 'react';
import VisibleTaskList from '../containers/VisibleTaskList'

export default class DisplayAllItems extends Component {
  render() {
    return (
      <div>
        <VisibleTaskList />
      </div>
    )
  }
}
